#include "one.h"

/**
 * main - Prints output into the standard output
 * @greeting: array of characters to fisplay on
 * the terminal/ stdin
 * 
 * Return: On success(0), on failure(1)
*/

int betty()
{
    char greeting[] = "Hello there";

    printf("%s", greeting);

    return(0);
}
